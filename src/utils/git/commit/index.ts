// https://gitlab.com/divramod/dm-tpl/issues/4

/**
 * commit({ message, issueNumber })
 * [ ]
 */
export async function commit({ message, issueNumber }) {

  console.log('commit') // tslint:disable-line:no-console

}
