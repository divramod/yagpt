/**
 *
 * @TODO
 * ```
 *
 * - [ ] write comments
 * - [ ] create tests
 * - [ ] createProgram() returns the created program
 * - [ ] getProgram()
 * - [ ] parse(removeArgsFromProcessArgv: string[]) lets the program parse the
 * process.argv
 * - [ ] removeArgsFromProcessArgv()
 *   - [ ] add UArray removeElementsFromStringArray()
 * ```
 */
export class ProgramUtility {

    /**
     * @param options
     * @TODO
     * ```
     *
     * - [ ] process
     * ```
     */
    constructor(
        options?: any,
    ) {
        //
    }
}
export const UProgram = new ProgramUtility()
export const UProgramClass = ProgramUtility
