// https://gitlab.com/divramod/dm-tpl/issues/4
import { expect } from 'chai'
import 'mocha'

import { main } from './'

describe.skip('index.ts', async () => {

    it('should start a job', async () => {
        // const R_RUN_MAIN = await main()

        // TEST
        expect(1).to.equal(1) // fails

    })

})
